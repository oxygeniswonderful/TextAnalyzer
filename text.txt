   if (blabla == null) { //4
 return 1; rofl 2; push 3; if(){ flex 4; //lol what is it?
chill 5; if() { /* import numpy as np
import pandas as pd
data = pd.read_csv(file, head = None)
data_numpy = data.to_numpy().flatten()
#Оценка стандартного отклонения (несмещенная)
print("Оценка стандартного отклонения (несмещенная) {:.2}"
      .format(statistics.stdev(data_numpy)*(len(data_numpy) / (len(data_numpy) - 1))**0.5)) */ mmm;}}}
